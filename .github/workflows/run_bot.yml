import os
import requests
import time
import sys

# دریافت توکن‌ها
GEMINI_KEY = os.getenv("GEMINI_KEY")
BALE_TOKEN = os.getenv("BALE_TOKEN")

def get_gemini_response(user_text):
    try:
        model = "gemini-1.5-flash"
        url = f"https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key={GEMINI_KEY}"
        payload = {"contents": [{"parts": [{"text": user_text}]}]}
        response = requests.post(url, json=payload, timeout=15)
        res_json = response.json()
        
        if response.status_code == 200:
            return res_json['candidates'][0]['content']['parts'][0]['text']
        else:
            return f"خطای گوگل: {res_json.get('error', {}).get('message', 'Error')}"
    except Exception as e:
        return f"خطا: {str(e)[:50]}"

def send_message(chat_id, text):
    url = f"https://tapi.bale.ai/bot{BALE_TOKEN}/sendMessage"
    try:
        requests.post(url, json={"chat_id": chat_id, "text": text}, timeout=10)
    except:
        pass

def bot_loop():
    last_update_id = 0
    # استفاده از flush=True برای نمایش فوری در گیت‌هاب
    print("--- ربات با موفقیت در گیت‌هاب استارت خورد ---", flush=True)
    
    start_time = time.time()
    # محدودیت ۶ ساعت برای جلوگیری از جریمه گیت‌هاب
    while time.time() - start_time < 21000: 
        try:
            url = f"https://tapi.bale.ai/bot{BALE_TOKEN}/getUpdates?offset={last_update_id + 1}&timeout=20"
            resp = requests.get(url, timeout=30)
            res = resp.json()
            
            if res.get("ok") and res.get("result"):
                for update in res["result"]:
                    last_update_id = update["update_id"]
                    if "message" in update and "text" in update["message"]:
                        chat_id = update["message"]["chat"]["id"]
                        text = update["message"]["text"]
                        print(f"پیام دریافت شد: {text[:10]}", flush=True)
                        reply = get_gemini_response(text)
                        send_message(chat_id, reply)
                        print(f"پاسخ ارسال شد.", flush=True)
        except Exception as e:
            print(f"خطا در حلقه: {e}", flush=True)
            time.sleep(10)
        time.sleep(1)

if __name__ == "__main__":
    bot_loop()
